<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Api.Buddy.Main.UI.MVVM"
             xmlns:controls="clr-namespace:Api.Buddy.Main.UI.Controls.Request"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Api.Buddy.Main.UI.Controls.Request.RequestView"
             x:CompileBindings="True"
             x:DataType="vm:RequestViewModel">
    <StackPanel Orientation="Vertical">
        <controls:RequestDetails DataContext="{Binding RequestInit}" SendCommand="{Binding ((vm:RequestViewModel)DataContext).SendCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:RequestView }}" />
        <controls:ResponseView DataContext="{Binding Response}" />
    </StackPanel>
</UserControl>
